1. pip install all requirements
pip install numpy
pip install scikit-learn
pip install scipy
pip install matplotlib
pip install tweepy
pip install textblob

2. run dashboard.py and check all the features.
3. u can delete tweet.db and cluster0-5.csv file but it is not necessary.
4. after deleting tweet.db, run dbCreate.py before u start.